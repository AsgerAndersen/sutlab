# Project: sutlab

## What this project does
Python library for compiling, balancing, and analysing supply and use tables (SUTs) in the Danish national accounts. Primary users are ~10 colleagues at Statistics Denmark with mixed Python experience, most with SAS backgrounds.

## Technology stack
- Language: Python 3.12
- Environment: UV — run Python with `uv run python` from the project root. No activation needed.
- Package install policy: always ask before adding new dependencies; use `uv add`
- Key dependencies: pandas, openpyxl (Excel for metadata/configuration), pyarrow (parquet support) — others to be decided

## Current status
- **Phase**: Implementation
- **What exists**: Core SUT dataclasses, `set_balancing_id`, and `filter_rows` (`sutlab/sut.py`) + tests (`tests/test_sut.py`) + metadata I/O functions and `load_sut_from_parquet` (`sutlab/io.py`) + tests (`tests/test_io.py`) + `inspect_products` (`sutlab/inspect/`) returning 15 tables (balance, supply/use detail, supply/use summary, price layers, price layer rates, and distribution/growth variants for all groups) + tests (`tests/test_inspect.py`, `tests/test_derive.py`) + `compute_price_layer_rates` (`sutlab/derive.py`) + `BalancingTargets`, `BalancingConfig`, `TargetTolerances`, `Locks` dataclasses + `set_balancing_targets`, `set_balancing_config` + `load_balancing_targets_from_excel`, `load_balancing_config_from_excel` + fixture data (`data/fixtures/`) + user documentation (`docs/`) + `balance_columns`, `balance_products_use` (`sutlab/balancing.py`) + tests (`tests/test_balancing.py`) + pandas-style methods on `SUT` delegating to all public non-loader free functions + `inspect_industries` (`sutlab/inspect/_industries.py`) returning 12 tables (balance, balance_growth, supply/use detail + distribution/coefficients/growth variants, price layers + rates/distribution/growth) + tests (`tests/test_inspect_industries.py`, `tests/test_price_layers_industries.py`) + `inspect_final_uses` (`sutlab/inspect/_final_uses.py`) returning 14 tables (`use` transaction-level, `use_categories` by transaction+category, `use_products` by transaction+category+product, each with `_distribution` and `_growth` variants, plus `use_products_summary` (per-(transaction, category) statistics aggregated over products; index `(transaction, transaction_txt, category, category_txt, summary)`), `price_layers`, `price_layers_rates`, `price_layers_distribution`, `price_layers_growth` — intermediate layers only, no basic prices, no total rows; `percentiles` (default `[0.5, 1.0]`) and `coverage_thresholds` (default `[0.5, 0.8, 0.95]`) keyword arguments) + tests (`tests/test_inspect_final_uses.py`) + `get_codes_products`, `get_codes_transactions`, `get_codes_industries`, `get_codes_individual_consumption`, `get_codes_collective_consumption` each return a `_txt` label column when the corresponding classification table is present, and accept an optional filter argument (`products`, `transactions`, `industries`, or `categories`) using the same pattern syntax as `filter_rows` + `as_list=False` (returns plain list of codes, omitting txt column) + `table=None` ("supply"/"use" restricts code discovery to that table; `get_codes_individual_consumption`/`get_codes_collective_consumption` with `table="supply"` return empty) + `inspect_unbalanced_products(sut, products=None, ids=None, tolerance=1)` (`sutlab/inspect/_product_imbalances.py`) returning `UnbalancedProductsInspection` with `.data.imbalances` and styled `.imbalances` property — one row per product where `abs(diff) > tolerance`; `ids=None` spans all ids, `ids=...` restricts using pattern API; no `balancing_id` requirement; index always MultiIndex (id outermost, then product, then label when available); styled `.imbalances` draws thick borders between id blocks; summary collapsed across ids; margin products always excluded; columns `diff_*`, `rel_*`, `supply_*`, `use_*`, price layers, `use_{purchasers}` + tests (`tests/test_inspect_product_imbalances.py`) + `SUTClassifications.margin_products` — optional DataFrame mapping margin-supply products to their price layer column; loaded from optional `margin_products` sheet in the classifications Excel file + `resolve_target_tolerances` (`sutlab/balancing/_tolerances.py`) attaching `tol_{price_basic}` / `tol_{price_purchasers}` columns to balancing targets + `inspect_unbalanced_targets(sut, transactions=None, categories=None, ids=None)` (`sutlab/inspect/_balancing_targets.py`) returning `UnbalancedTargetsInspection` with `.data.supply_categories`, `.data.use_categories`, `.data.supply_transactions`, `.data.use_transactions`, `.data.supply_categories_violations`, `.data.use_categories_violations`, `.data.supply_transactions_violations`, `.data.use_transactions_violations`; `ids=None` spans all ids, `ids=...` restricts using pattern API; no `balancing_id` requirement; all tables have id as outermost index level; summary collapsed across ids + tests (`tests/test_inspect_unbalanced_targets.py`) + `adjust_add_sut(sut, adjustments)` (`sutlab/adjust/`) — adds values from one SUT to another; matching keys summed, new keys appended, NaN treated as 0; balancing targets combined with same semantics when present; primary use case: benchmark revision adjustments + tests (`tests/test_adjust.py`) + `load_sut_from_dataframe`, `load_balancing_targets_from_dataframe` (`sutlab/io.py`) — accept combined in-memory DataFrames (id column present, all years stacked); price columns must already be numeric + tests (`tests/test_io.py`) + `load_balancing_targets_from_separated_parquet`, `load_balancing_targets_from_combined_parquet`, `load_balancing_targets_from_separated_csv`, `load_balancing_targets_from_combined_csv` (`sutlab/io.py`) — parquet and CSV equivalents of the existing Excel balancing targets loaders; same semantics (no `price_basis` argument, `print_paths` supported, `sep`/`encoding` on CSV variants) + tests (`tests/test_io.py`) + `compute_totals(sut, dimensions)` (`sutlab/derive.py`) — sums price columns over one or more key dimensions; `dimensions` specifies which of `{product, transaction, category}` to keep (id always kept); returns stacked DataFrame (combined format) with NaN preserved for all-NaN groups (`min_count=1`); accepts string or list; callable as `sut.compute_totals(dimensions)` + tests (`tests/test_derive.py`) + `print_paths: bool = False` (keyword-only) on all loader and writer functions in `io.py` and the six `SUT` write delegate methods — when `True`, prints paths being read/written before the I/O operation; price basis included in SUT messages, omitted from metadata/balancing config/balancing targets messages + tests (`tests/test_io.py`) + `use_price_columns: str | list[str] | None = None` (keyword-only) on `compute_totals` — when specified, only those price columns (actual column names) are summed for use rows; all others NaN; supply rows unaffected + tests (`tests/test_derive.py`) + `sheet_name: str | int = 0` (keyword-only) on all four Excel SUT and balancing targets loaders — reads from a named or indexed sheet; same sheet applied to every file in the separated loaders; default `0` (first sheet) preserves existing behaviour + tests (`tests/test_io.py`) + `inspect_sut_comparison(before, after, ...)` (`sutlab/inspect/_sut_comparison.py`) returning `SUTComparisonInspection` with `data: SUTComparisonData` (8 comparison DataFrames + `summary` + 4 aggregate summary DataFrames); outer-join row-level diff across all price columns; 4 SUT tables (`supply`, `use_basic`, `use_price_layers`, `use_purchasers`) + 4 nullable balancing targets tables; filtering by ids/products/transactions/categories; `diff_tolerance`, `rel_tolerance` (AND logic — both must be exceeded; `rel_tolerance=inf` disables rel check), `filter_nan_as_zero` arguments; `percentiles: list[float] = [0.0, 0.5, 1.0]` controls percentile columns on summary tables; `supply_products_summary`, `supply_columns_summary`, `use_products_summary`, `use_columns_summary` — each with `n_changes`, `diff_norm`, diff/rel percentile columns; `.summary` has blocks: base tables → products → columns → balancing targets, column `n_changes`; styled properties with supply=green/use=blue/layers=cycling palettes + tests (`tests/test_inspect_sut_comparison.py`) + `write_to_excel(path)` on all six inspection result classes — writes each non-None DataFrame field as a named sheet; uses Styler where available (colours, bold, number formats); openpyxl post-processing: bold headers, index column widths fit-to-content, value column width fixed at 13, number/percentage formats (`#,##0.0` / `0.0%`) detected from field name and column name + tests (`tests/test_inspect_write_to_excel.py`) + `inspect_aggregates_nominal(sut, gdp_decomp=None)` (`sutlab/inspect/_aggregates_nominal.py`) returning `AggregatesNominalInspection` with `.data.gdp`, `.data.gdp_growth`, `.data.gdp_distribution` — nominal GDP decomposition table, 2-level MultiIndex (block, component), columns = id values; Production block (P1 − P2 + product taxes + import duties), Expenditure block (domestic final use + net exports), Balance block (Production GDP − Expenditure GDP); styled `.gdp` (number format), `.gdp_growth` (year-on-year rates, percentage format, Balance excluded), `.gdp_distribution` (shares of block GDP, percentage format, Balance excluded) properties with block colours, bold GDP, italic derived rows, thick block separators; `gdp_decomp` column on transactions classification drives row labels, overridable via argument + tests (`tests/test_inspect_aggregates_nominal.py`) + all 7 inspection result classes carry a `display_configuration: DisplayConfiguration` (in `sutlab/inspect/_display_config.py`) with setters: `set_display_unit` (validates positive power of 10), `set_display_rel_base` (100/1000/10000, maps to %/‰/‱), `set_display_decimals` (non-negative int), `set_display_index(level, values)` (filter applied on styled properties only, not `.data`; replaces existing values for that level, or adds the level if not present), `set_display_sort_column(column, ascending=False)`, `set_display_values_n_largest(n, column)`, `set_display_configuration_to_defaults()`; all return new copies; none are arguments to the inspect functions; `DisplayConfiguration` exported from `sutlab/inspect/__init__.py` + all 7 inspection result classes have `inspect_tables_comparison(other)` returning a `TablesComparison` with `.diff` and `.rel` as instances of the same class; `.diff` holds element-wise differences (self − other), `.rel` holds relative changes ((self − other) / other) formatted as percentages; outer-join alignment; None fields propagate; `TablesComparison` setters propagate to both inner objects via their `set_display_*` methods; `TablesComparison` exported from `sutlab/inspect/__init__.py` + tests (`tests/test_inspect_tables_comparison.py`)
- **What's next**: `inspect_aggregates_real` (chain-linked volume indices — chain-linking is not additive, rows must be chained independently); further inspection/display functions for industries; `_shared.py` also now holds `_percentile_label` and `_build_summary_table` + `aggregate_classification_products`, `aggregate_classification_transactions`, `aggregate_classification_industries`, `aggregate_classification_individual_consumption`, `aggregate_classification_collective_consumption` (`sutlab/aggregate/`) — all five exist and are tested (`tests/test_aggregate.py`)

## Architecture

### Module structure
- `sutlab/sut.py` — Core dataclasses: `SUT`, `SUTMetadata`, `SUTColumns`, `SUTClassifications`, `BalancingTargets`, `BalancingConfig`, `TargetTolerances`, `Locks`; and `set_balancing_id`, `set_balancing_targets`, `set_balancing_config`, `set_metadata`, `filter_rows`, `get_codes_products`, `get_codes_transactions`, `get_ids`, `get_codes_industries`, `get_codes_individual_consumption`, `get_codes_collective_consumption`; pandas-style methods on `SUT` delegating to all public non-loader free functions including `write_to_separated_parquet`, `write_to_combined_parquet`, `write_to_separated_csv`, `write_to_combined_csv`, `write_to_separated_excel`, `write_to_combined_excel`, `aggregate_classification_products`, `aggregate_classification_transactions`, `aggregate_classification_industries`, `aggregate_classification_individual_consumption`, `aggregate_classification_collective_consumption`
- `sutlab/aggregate/` — Package. `__init__.py` re-exports the public API. `_shared.py` holds all dimension-agnostic helpers: `_validate_mapping`, `_validate_full_coverage(mapping, dfs, col)`, `_validate_from_in_classification(mapping, cls_df, col, cls_label)`, `_validate_metadata_columns_simple(metadata, col)`, `_validate_transactions_metadata_columns(metadata, col)` (requires `table` and `esa_code` columns), `_validate_no_passthrough_collision(mapping, dfs, col)`, `_aggregate_long_table(df, mapping, key_cols, col)`, `_aggregate_with_esa_filter(df, mapping, key_cols, col, trans_col, matching_trans)` (aggregates only ESA-relevant rows; other rows pass through unchanged), `_build_classification(old_cls_df, mapping, metadata, full_coverage, col)`, `_update_classification_names`, `_get_matching_trans(trans_cls, trans_col, esa_codes)`, `_require_transactions_classification(sut, function_name)`. `_products.py` holds `aggregate_classification_products(sut, mapping, *, metadata=None, full_coverage=True, classification_name=None)` → `SUT`; `balancing_targets` pass through unchanged (no product column in targets); `balancing_config` cleared; `margin_products` remapped (mixed or inconsistent-layer sources raise). `_transactions.py` holds `aggregate_classification_transactions` — all rows in scope; `balancing_targets` aggregated with same mapping; `metadata` must include `table` and `esa_code` columns. `_industries.py` holds `aggregate_classification_industries` (ESA P1/P2). `_individual_consumption.py` holds `aggregate_classification_individual_consumption` (ESA P31). `_collective_consumption.py` holds `aggregate_classification_collective_consumption` (ESA P32). For all three category functions: aggregation and validation scoped to ESA-relevant rows via `_aggregate_with_esa_filter`; `transactions` classification required (raises if absent); `balancing_targets` aggregated on same ESA-filtered rows. All five functions: `balancing_config` cleared, `balancing_id` preserved. New aggregate functions get their own `_<name>.py`.
- `sutlab/adjust/` — Package. `__init__.py` re-exports the public API. `_add.py` holds `adjust_add_sut(sut, adjustments)` → `SUT` — adds values from one SUT to another (matching keys summed, new keys appended, NaN treated as 0; balancing targets combined with same semantics). New adjust functions get their own `_<name>.py`.
- `sutlab/io.py` — I/O functions (public): `load_metadata_from_excel`, six SUT loaders (all sort supply and use rows by id, product, transaction, category): `load_sut_from_separated_parquet(id_values, paths, metadata, price_basis)`, `load_sut_from_combined_parquet(path, metadata, price_basis)`, `load_sut_from_separated_csv(id_values, paths, metadata, price_basis, *, sep, encoding)`, `load_sut_from_combined_csv(path, metadata, price_basis, *, sep, encoding)`, `load_sut_from_separated_excel(id_values, paths, metadata, price_basis)`, `load_sut_from_combined_excel(path, metadata, price_basis)`. Six SUT writers: `write_sut_to_separated_parquet(sut, id_values, paths)`, `write_sut_to_combined_parquet(sut, path)`, `write_sut_to_separated_csv(sut, id_values, paths, *, sep, encoding)`, `write_sut_to_combined_csv(sut, path, *, sep, encoding)`, `write_sut_to_separated_excel(sut, id_values, paths)`, `write_sut_to_combined_excel(sut, path)`. Separated writers take explicit id values and output paths (caller controls naming); combined writers take a single output path (caller controls naming). Separated writers validate that all provided id values are present in the SUT. Supply and use are concatenated on write; supply rows have NaN in price layer and purchasers' price columns. "Separated" = one file per member, id column absent; "combined" = one file for all members, id column present. CSV/Excel loaders read product/transaction/category as str and convert price columns to numeric. `load_sut_from_dataframe(df, metadata, price_basis)`, `load_balancing_targets_from_separated_parquet(id_values, paths, metadata)`, `load_balancing_targets_from_combined_parquet(path, metadata)`, `load_balancing_targets_from_separated_csv(id_values, paths, metadata, *, sep, encoding)`, `load_balancing_targets_from_combined_csv(path, metadata, *, sep, encoding)`, `load_balancing_targets_from_separated_excel(id_values, paths, metadata)`, `load_balancing_targets_from_combined_excel(path, metadata)`, `load_balancing_targets_from_dataframe(df, metadata)`, `load_balancing_config_from_excel(metadata, *, tolerances_path, locks_path)`. Six balancing targets writers: `write_balancing_targets_to_separated_parquet(targets, id_values, paths, columns_metadata)`, `write_balancing_targets_to_combined_parquet(targets, path, columns_metadata)`, `write_balancing_targets_to_separated_csv(targets, id_values, paths, columns_metadata, *, sep, encoding)`, `write_balancing_targets_to_combined_csv(targets, path, columns_metadata, *, sep, encoding)`, `write_balancing_targets_to_separated_excel(targets, id_values, paths, columns_metadata)`, `write_balancing_targets_to_combined_excel(targets, path, columns_metadata)`. All take `columns_metadata: SUTColumns` (not full metadata — no classifications needed for writing). `BalancingTargets` exposes matching `write_to_*` delegate methods. Sub-loaders, `_assemble_sut`, `_assemble_balancing_targets`, `_combine_supply_use`, and `_combine_balancing_targets` are private helpers.
- `sutlab/derive.py` — Derived analytical quantities: `compute_price_layer_rates(sut, aggregation_level)` — computes step-wise price layer rates; `aggregation_level` accepts a role string or list of role strings (e.g. `"product"`, `["transaction", "category"]`); uses hardcoded Danish default denominators; raises on unsupported layers. `compute_totals(sut, dimensions)` — sums price columns keeping id plus the specified dimensions; `dimensions` accepts a role string or list; supply rows retain NaN for price layer and purchasers' price columns. Future: chain-linked volume indices, GDP components.
- `sutlab/inspect/` — Package. `__init__.py` re-exports the public API including `DisplayConfiguration`. `_style.py` holds all formatting helpers, colour constants, and Styler factories. `_shared.py` holds shared helpers: `_sort_by_id_value`, `_make_sheet_name`, `_apply_bold_headers`, `_apply_number_formats`, `_fit_index_column_widths`, `_set_value_column_widths`, `_write_inspection_to_excel`, `_build_growth_table` (pure math: `(current−previous)/previous`, inf→NaN; row filtering is the caller's responsibility), `_percentile_label`, `_build_summary_table(detail_table, group_levels, item_levels, item_count_label, total_label, percentiles, coverage_thresholds)` (builds per-group summary statistics; non-total filter = `transaction != ""`; used by both `_products.py` and `_industries.py`), `_apply_display_config(df, table_name, config)` (applies display_index filter → n_largest filter → sort; returns df unchanged if table_name in config.protected_tables or df is empty), and related `_apply_display_index_filter`, `_apply_n_largest_filter`, `_apply_column_sort`, `_is_protected_row_mask`, `_iter_group_positions` helpers. All seven inspection result classes expose `write_to_excel(path)` delegating to `_write_inspection_to_excel`: writes each non-None DataFrame field as a sheet via `Styler.to_excel()` (with silent fallback to raw), then applies openpyxl post-processing (bold headers, index column width fit-to-content, value column fixed width 13, number/percentage formats). `_display_config.py` holds `DisplayConfiguration` dataclass and `_cfg_*` setter helpers used by all 7 inspection classes and `TablesComparison`. `_products.py` holds `inspect_products(sut, products, ids=None, *, percentiles=[0.5,1.0], coverage_thresholds=[0.5,0.8,0.95])` → `ProductInspection` (15 tables: balance, supply/use detail, supply_summary/use_summary, price_layers, price_layers_rates, and distribution/growth variants for all groups). Balance and use detail at purchasers' prices; detail tables include uncategorized transactions and per-product Total rows. `supply_summary`/`use_summary`: per-product statistics across (transaction, category) rows — index `(product, product_txt, summary)`, rows: `total_supply`/`total_use`, `n_categories`, `n_categories_pX` (coverage), `value_*`/`share_*` percentiles. Shared helper `_build_summary_table` in `_shared.py` (also used by `_industries.py`) parameterised on `group_levels`, `item_levels`, `item_count_label`. `_industries.py` holds `inspect_industries(sut, industries, ids=None, *, percentiles=[0.5,1.0], coverage_thresholds=[0.5,0.8,0.95])` → `IndustryInspection` (15 tables: balance, balance_growth, supply_products, supply_products_distribution, supply_products_growth, supply_products_summary, use_products, use_products_distribution, use_products_coefficients, use_products_growth, use_products_summary, price_layers, price_layers_rates, price_layers_distribution, price_layers_growth). Balance table rows: P1 transactions (basic prices), optional Total output (if ≥2 P1 in metadata), P2 transactions (purchasers' prices), optional Total input (if ≥2 P2 in metadata), GVA, input coefficient. `price_layers_distribution` empty when only 1 P2 transaction. `supply/use_products_summary`: per-transaction statistics (total, n_products, coverage counts `n_products_p{N}`, value percentiles, share percentiles); row order: total, n_products, coverage (asc), value (desc), share (desc); formatting: total/value→number, n_products*→int, share→percentage. `_final_uses.py` holds `inspect_final_uses(sut, transactions, *, categories=None, ids=None)` → `FinalUseInspection` (13 tables: `use`, `use_categories`, `use_products`, each with `_distribution`/`_growth` variants, plus 4 price layer tables). Candidate transactions: use-side rows with ESA codes excluding P2. `_NON_FINAL_USE_ESA_CODES = {"P2"}`. `_product_imbalances.py` holds `inspect_unbalanced_products(sut, products=None, ids=None, tolerance=1)` → `UnbalancedProductsInspection` (1 table: `imbalances`). No `balancing_id` requirement; `ids=None` spans all ids. Index always a MultiIndex (id outermost, then product, then label if available). Thick borders between id blocks in styled `.imbalances`. Summary collapsed across ids. Margin products always excluded. `_resolve_ids` and `_prepend_id_level` are private helpers. `_balancing_targets.py` holds `inspect_unbalanced_targets(sut, transactions=None, categories=None, ids=None)` → `UnbalancedTargetsInspection` (4 main tables + 4 optional violations tables). No `balancing_id` requirement; `ids=None` spans all ids; all tables have id as outermost index level; summary collapsed across ids. Columns: `{price}`, `target_{price}`, `diff_{price}`, `rel_{price}`, `tol_{price}`, `violation_{price}`. Supply uses basic prices; use uses purchasers' prices. Tolerances resolved silently if `tol_` columns absent; `None` violations tables when no `target_tolerances` configured. Styled with transaction block separators. `_build_categories_table` and `_build_transactions_table` take `id_value` explicitly. `_resolve_ids` and `_prepend_id_level` are private helpers. `_sut_comparison.py` holds `inspect_sut_comparison(before, after, *, ids, products, transactions, categories, diff_tolerance, rel_tolerance, filter_nan_as_zero, percentiles)` → `SUTComparisonInspection` with `data: SUTComparisonData` (8 comparison DataFrames + `summary` + 4 aggregate summary DataFrames). Tables: `supply`, `use_basic`, `use_price_layers`, `use_purchasers` (SUT data); `balancing_targets_supply`, `balancing_targets_use_basic`, `balancing_targets_use_price_layers`, `balancing_targets_use_purchasers` (all `None` when either SUT lacks targets); `supply_products_summary`, `supply_columns_summary`, `use_products_summary`, `use_columns_summary` (derived from filtered `supply`/`use_purchasers`; columns: `n_changes`, `diff_norm`, diff/rel percentile columns). `diff_tolerance` and `rel_tolerance` use AND logic. Styled properties: supply=green, use=blue, layers=cycling palettes; `before_*` shade 0, `after_*` shade 1; id-block separators. `summary` column `n_changes`; blocks: base tables → products → columns → balancing targets; styled with supply=green, use=alternating blue, block separators. New inspection functions get their own `_<name>.py` with public names re-exported in `__init__.py`. `_tables_comparison.py` holds `TablesComparison` dataclass and `_compute_comparison_table_fields` helper. `_aggregates_nominal.py` holds `inspect_aggregates_nominal(sut, gdp_decomp=None)` → `AggregatesNominalInspection` with `.data.gdp`, `.data.gdp_growth`, `.data.gdp_distribution` (all 2-level MultiIndex DataFrames: level 0 = block name, level 1 = component label; columns = sorted id values). Production block: P1 output rows (basic prices), P2 intermediate consumption rows (purchasers' prices, negated), Gross Value Added (derived), product tax/subsidy rows (one per present column), import duties (D2121, if present), Total product taxes netto (derived), GDP (derived). Expenditure block: domestic final use rows (use-side, esa_code not P2/P6, purchasers' prices), Domestic final expenditure (derived), P6 export rows, P7 import rows (negated), Export netto (derived), GDP (derived). Balance block: single GDP row = Production GDP − Expenditure GDP (present in `gdp` only; excluded from `gdp_growth` and `gdp_distribution`). `gdp_growth`: year-on-year rates via `_build_growth_table`. `gdp_distribution`: Production rows ÷ Production GDP, Expenditure rows ÷ Expenditure GDP. Styled properties: Production=supply green, Expenditure=use blue, Balance=grey; GDP rows bold; GVA/Total product taxes netto/Domestic final expenditure/Export netto italic with thin borders above/below; thick borders between blocks; `.gdp` uses number format, `.gdp_growth`/`.gdp_distribution` use percentage format. `gdp_decomp` column on `SUTClassifications.transactions` maps transaction codes to component labels; optional `gdp_decomp` argument overrides it.
- `sutlab/balancing/` — Package. `__init__.py` re-exports the public API. `_shared.py` holds `_evaluate_locks`, `_get_use_price_columns`. `_columns.py` holds `balance_columns(sut, transactions=None, categories=None, adjust_products=None)` → `SUT` — scales adjustable rows to hit column targets; transaction/category locks skip silently; product/cell locks covering all adjustable rows raise an informative error. `_products_use.py` holds `balance_products_use(sut, products=None, adjust_transactions=None, adjust_categories=None)` → `SUT` — scales use rows so each product's total use in basic prices matches its supply total; target derived from supply; all price columns scaled to preserve price layer rate ratios. `_tolerances.py` holds `resolve_target_tolerances(sut) → SUT` — attaches `tol_{price_basic}` / `tol_{price_purchasers}` columns to balancing targets; tolerance = `min(abs(rel*target), abs_tol)` with partial tolerance support (rel-only or abs-only); raises only when both are absent for a non-NaN target. `_filter_free.py` holds `filter_free_cells(sut, *, table=None)` → `SUT` — removes locked rows from supply and/or use; `table="supply"` or `"use"` limits filtering to that table, `None` (default) filters both. New balancing functions (RAS, price layer balancing, VAT threshold) get their own `_<name>.py`.

### Core data representation

**`SUT`** — top-level object, holds a collection of supply and use tables:
- `price_basis`: `"current_year"` or `"previous_year"`. Current and previous year prices
  are kept as **separate SUT objects**; the same metadata can be shared between them.
- `supply`: long-format DataFrame, all collection members, basic prices only
- `use`: long-format DataFrame, all collection members, all price columns
- `balancing_id`: id value of the member currently being balanced, or `None`
- `balancing_targets`: optional `BalancingTargets` — target column totals for balancing
- `balancing_config`: optional `BalancingConfig` — tolerances and locked cells
- `metadata`: optional `SUTMetadata`

The collection design separates two workflows: **inspection** spans the full collection;
**balancing** operates only on the member identified by `balancing_id`. This keeps a
multi-year series available as context during single-year balancing.

**`SUTMetadata`**
- `columns`: `SUTColumns` — required
- `classifications`: `SUTClassifications` — optional

**`SUTColumns`** — maps conceptual roles to actual DataFrame column names. Loaded from a
two-column Excel table (`column`, `role`). Required roles: `id`, `product`, `transaction`,
`category`, `price_basic`, `price_purchasers`. Optional roles (all `str | None`):
`trade_margins`, `wholesale_margins`, `retail_margins`, `transport_margins`,
`product_taxes`, `product_subsidies`, `product_taxes_less_subsidies`, `vat`.

**`SUTClassifications`** — optional classification tables, all fields `DataFrame | None`.
- `classification_names` — maps dimension names to classification system names; `dimension` and `classification` columns
- `products` — key column named after the actual product column (e.g. `nrnr`), label column named `{col}_txt` (e.g. `nrnr_txt`)
- `transactions` — key column named after the actual transaction column (e.g. `trans`), label column `trans_txt`, plus `table` and `esa_code`; `table` is `"supply"` or `"use"`, required and validated on load. Used to split the combined parquet file into supply and use tables. Optional extra column `gdp_decomp` maps transaction codes to GDP decomposition labels; used by `inspect_aggregates_nominal`.
- `industries`, `individual_consumption`, `collective_consumption` — key column named after the actual category column (e.g. `brch`), label column `brch_txt`. These three all live in the `category` column of the data — which classification applies depends on the transaction code (P1/P2 → industries, P31 → individual_consumption, P32 → collective_consumption).
- `margin_products` — products whose supply represents a trade margin. Columns: `{product_col}`, optionally `{product_col}_txt`, and `price_layer` (actual data column name, e.g. `handelsm`). Loaded from optional `margin_products` sheet in the classifications Excel file; `price_layer` values validated against known price layer columns from `SUTColumns`. These products are always excluded from `inspect_unbalanced_products`.

**`BalancingTargets`** — target column totals, split into supply and use. Mirrors the SUT
long-format without the product dimension. Supply: `id, transaction, category, price_basic`.
Use: `id, transaction, category, price_basic, [price layers], price_purchasers`. NaN in a
price column means no target for that combination.

**`BalancingConfig`** — balancing configuration independent of which id is being balanced:
- `target_tolerances`: optional `TargetTolerances` — `transactions` and `categories` DataFrames
  (columns: transaction/category col names, `rel`, `abs`). Loaded from Excel with sheets
  `transactions` and `categories`.
- `locks`: optional `Locks` — `products`, `transactions`, `categories`, `cells`, `price_layers`
  DataFrames. A cell is locked if it matches any of the first four levels (OR logic).
  `price_layers` has a single `price_layer` column; listed layers are excluded from scaling
  in all balancing functions (values held fixed; implied rates allowed to change). Validated
  on load against known price layer column names from metadata. Loaded from Excel; all sheets
  optional — silently absent if the sheet does not exist.

**`set_balancing_id / set_balancing_targets / set_balancing_config / set_metadata`** — each returns a new
SUT with one field updated. Does not mutate the original.

### Design principles
- Readability over elegance or performance — target reader has limited Python experience
- Explicit over concise — avoid abstractions that obscure what the code is doing
- Break multi-step operations into named intermediate variables rather than chaining
- Functional style — plain functions that take data and return data; no class hierarchies
- Dataclasses are fine as simple data containers
- Informative error messages: `"Product 'X' not found. Available: ..."` not bare KeyError
- API design: prefer many small public functions with few arguments over fewer abstract
  functions with many arguments. Names should be explicit and hierarchically structured
  so related functions group together in autocomplete. Users navigate the API primarily
  by name. Sub-loaders and other internal helpers are private (`_` prefix) — only
  top-level loaders are public. This principle applies to the public API only.
- Use native pandas operations (groupby, merge, vectorised column ops) over Python loops on DataFrame rows or ids — both for performance and readability
- Always use `dropna=False` in `groupby` calls — the default `dropna=True` silently drops NaN group keys (e.g. empty category values)
- Never rely on column or row ordering — always identify columns by name via `SUTColumns` and select rows by value, not by position
- Column names never hardcoded — always via `SUTColumns`
- Supply holds only basic prices; price layers are a use-side concept
- DataFrame column order (established by I/O functions, not enforced by dataclass):
  - Supply: `id, product, transaction, category, price_basic`
  - Use: `id, product, transaction, category, price_basic, [price layers], price_purchasers`
- Type hints and NumPy-style docstrings on all public functions

### Scope exclusions (current)
- Value added not decomposed — GVA is derivable as output minus intermediate use, but no
  breakdown by wages, operating surplus, etc.
- No supplementary data (hours worked, employment, capital stock)

## Domain glossary
- **SUT**: Supply and use table — matrix framework balancing production and use of goods
  and services
- **Basic prices / purchasers' prices**: The two main price bases. The difference is
  accounted for by price layers: wholesale margins, retail margins, taxes less subsidies
  on products (excluding VAT), and VAT. Danish-specific — do not assume the simpler SNA
  treatment.
- **Transaction codes**: SNA codes identifying economic flows (e.g. P1 output, P2
  intermediate consumption). Never hardcoded — always user-supplied.
- **Current / previous year's prices**: Current = prices of year t. Previous = values for
  year t revalued at prices of t−1. Used for volume calculations.
- **Chain-linked volume indices**: Derived from current and previous year's prices tables
  by linking year-to-year Laspeyres indices. Chaining and aggregation do not commute —
  aggregate first, then chain.

## Data
- `data/examples/` → Example SUT data from Statistics Denmark (parquet + Excel metadata)
- `data/fixtures/` → Small synthetic data for tests (generated by `data/fixtures/generate.py`)

## Open design questions
- What other inspection functions are needed beyond what exists?
- `inspect_aggregates_real` interface unsettled — chain-linking rows independently, shared row-structure helper with nominal.
- What further balancing functions are needed beyond `balance_columns` and `balance_products_use`?
- `derive.py` scope settled (see decisions.md 2026-03-31). Convert to a package if/when the file grows crowded.
- What other functions belong in the adjust module (`adjust_multiply`, `adjust_move`)? Full scope unsettled pending user discussions.

## Project structure
CLAUDE.md is the authoritative record of the current state. `notes/claude/` holds
supplementary material — consult proactively when starting work on a topic with prior
design history, or when instructed.

Version control: git, remote on GitHub. Commit logical units of work with descriptive messages.

Claude can read and write: `sutlab/`, `tests/`, `notes/claude/`
Claude should NOT read or write: `notes/mine/`

Claude should NOT:
- Make architectural or structural decisions unilaterally — propose, then wait for approval
- Reorganise modules without discussion
- Add dependencies without asking
- Write implementation code during planning sessions (small illustrative sketches are fine)
- Push to GitHub without asking

## Session instructions

### Start of every session:
1. Read this CLAUDE.md in full
2. State briefly: current phase, what was last worked on, what's next
3. Consult `notes/claude/` when starting work on a topic with prior design history

### End of every session:
1. Append decisions made this session to `notes/claude/decisions.md`
2. Update any relevant topic reference files in `notes/claude/`
3. Update **Current status** and **Open design questions** in this file
4. Suggest any other CLAUDE.md edits for approval — do not edit directly

### Planning sessions:
- Propose options with explicit tradeoffs; do not advocate unless asked
- End with a clear statement of what has and hasn't been settled

### Implementation sessions:
- Check that relevant architecture decisions are settled before writing code
- Write tests alongside implementation, not after
- If a decision turns out to be ambiguous mid-implementation, stop and flag it
- Flag code that feels fragile, surprising, or assumption-laden, even if not asked

### General behaviour:
- When uncertain, say so explicitly rather than inferring silently
- Read source before making claims about function signatures or behaviour
- Prefer proposing over doing for anything structural
- Prefer targeted edits over rewrites
